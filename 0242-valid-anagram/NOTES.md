​use hashmap here: sort string and store it in map as key then iterate array is it has same key push into the existing key else create new key and store in map .Finally return array from map.values()
